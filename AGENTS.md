# Agent Instructions

Read `SKILL.md` before taking action. Use `scripts/authenticated_sharepoint_download.cjs` as the deterministic template for authenticated SharePoint batch downloads.

Keep credentials out of prompts and logs. Let the user complete Microsoft login/MFA in a temporary browser profile.
