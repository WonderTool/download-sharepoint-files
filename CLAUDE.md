# Claude Code Instructions

Read `SKILL.md` first. For implementation, copy or adapt `scripts/authenticated_sharepoint_download.cjs`, fill the `items` array, and run it with Node.js plus Playwright.

Do not ask users to paste Microsoft passwords, MFA codes, cookies, or tokens.
